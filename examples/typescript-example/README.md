# chatjs-typescript-demo

A simple React app with TypeScript support, bundled to ES6 via Rollup.

## Commands

```
npm start      # watch + dev server at http://localhost:3000 (auto-opens, livereloads)
npm run build  # production ES6 bundle → dist/
```
